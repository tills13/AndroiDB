<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.sbstn.dbtest" >

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ViewDataActivity"
            android:label="@string/title_activity_table"
            android:parentActivityName=".activity.DatabaseActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.DatabaseActivity" />
        </activity>
        <activity
            android:name=".activity.CreateOrEditServerActivity"
            android:label="@string/title_activity_add_database"
            android:parentActivityName=".activity.MainActivity"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.MainActivity" />
        </activity>
        <activity
            android:name=".activity.QueryRunnerActivity"
            android:label="@string/title_activity_query_runner" >
        </activity>
        <activity
            android:name=".activity.RowInspectorActivity"
            android:label="@string/title_activity_row_inspector" >
        </activity>
        <activity
            android:name=".activity.TableActivity"
            android:label="@string/title_activity_table" >
        </activity>
        <activity
            android:name=".activity.EditTableActivity"
            android:label="@string/title_activity_edit_table" >
        </activity>
    </application>

</manifest>
